linters:
  enable:
    - errorlint
    - err113
    - exhaustruct
    - gocritic
    - nilnil
    - nlreturn
    - revive
    - wrapcheck
    - wsl_v5
  settings:
    wrapcheck:
      ignore-package-globs:
        - github.com/couchbase/internal
        - crypto/x509
      ignore-interface-regexps:
        - "queryRowReader"
        - "queryClient"
        - "clusterClient"
  exclusions:
    presets:
      - std-error-handling
version: "2"
